<Window x:Class="csharp_project.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:csharp_project.Views"
        mc:Ignorable="d"
        Title="Знаки Зодіаку" Height="350" Width="400">

    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Оберіть дату народження:" FontSize="14"/>
            <DatePicker SelectedDate="{Binding SelectedDate, Mode=TwoWay}" />

            <TextBlock Text="Західний знак зодіаку:" FontSize="14" Margin="0,10,0,0"/>
            <TextBlock Text="{Binding ZodiacSign}" FontSize="16" FontWeight="Bold"/>

            <TextBlock Text="Китайський знак зодіаку:" FontSize="14" Margin="0,10,0,0"/>
            <TextBlock Text="{Binding ChineseZodiac}" FontSize="16" FontWeight="Bold"/>

            <TextBlock Text="Вік:" FontSize="14" Margin="0,10,0,0"/>
            <TextBlock Text="{Binding AgeText}" FontSize="16" FontWeight="Bold"/>

            <TextBlock Text="{Binding BirthdayMessage}" FontSize="16" FontWeight="Bold" Foreground="Red" Margin="0,10,0,0"/>


            <Button Content="Очистити" Command="{Binding ClearCommand}" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>
